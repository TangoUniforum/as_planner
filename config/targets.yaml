# Harvest targets — judged by Analyze's checklist ONLY when values are present.
# OPERATOR PRINCIPLE (2026-08-09): empty targets = the pipeline's results stand
# UNADULTERATED (the targets gate reports N/A, never judges). After reviewing
# results, enter real contract numbers here or in Configure -> Targets & prices
# to overlay judgment — edits re-judge existing results instantly, no re-runs.
basis: hog
tolerance_pct: 5.0
monthly: {}
yearly: {}
